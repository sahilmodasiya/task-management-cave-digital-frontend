export const API_BASE_URL = 'https://task-management-cave-digital-backend.onrender.com';

export const API_ENDPOINTS = {
  SIGNUP: '/auth/signup',
  LOGIN: '/auth/login',
  TASKS: '/tasks',
};

export const STORAGE_KEYS = {
  TOKEN: '@task_manager_token',
  USER: '@task_manager_user',
}; 